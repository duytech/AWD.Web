{
  "version": "1.0.0-*",

  "buildOptions": {
    // Currently, with .net core 1.1, on windows we still need this config for debugging.
    "debugType": "portable"
  },

  "dependencies": {
    "NETStandard.Library": "1.6.1"
  },

  "frameworks": {
    "netstandard1.6": {
      "imports": "dnxcore50"
    }
  }
}
